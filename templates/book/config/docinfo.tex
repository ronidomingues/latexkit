% =====================================================================
%  Liga os metadados do latexkit.config.json aos comandos da memoir.
%
%  As macros \lkAlgumaCoisa vem de config/metadata.tex, que e gerado.
%  Para mudar um valor, edite latexkit.config.json.
% =====================================================================

\title{\lkTitle}
\author{\lkAuthor}
\date{\lkYear}

\hypersetup{
  pdftitle={\lkTitle},
  pdfauthor={\lkAuthor},
  pdfsubject={\lkSubtitle},
  pdfcreator={latexkit}
}
